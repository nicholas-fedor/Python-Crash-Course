# A few Python functions are helpful when working with lists of numbers.
# For example, finding min, max, and sum is simple.

digits = [1,2,3,4,5,6,7,8,9,0]

print(min(digits)) # 0
print(max(digits)) # 9
print(sum(digits)) # 45